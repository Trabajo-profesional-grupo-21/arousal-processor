apiVersion: apps/v1
kind: Deployment
metadata:
  name: arousal-processor
spec:
  replicas: 5
  selector:
    matchLabels:
      app: arousal-processor
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: arousal-processor
    spec:
      containers:
      - name: arousal-processor
        image: <IMAGE>
        imagePullPolicy: IfNotPresent
        resources:
          limits:
            cpu: "1000m" # Límite máximo de uso de CPU
            memory: "1000Mi" # Límite máximo de uso de memoria
          requests:
            cpu: "500m" # Cantidad mínima de CPU requerida
            memory: "500Mi" # Cantidad mínima de memoria requerida
